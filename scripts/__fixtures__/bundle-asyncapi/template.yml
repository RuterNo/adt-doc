asyncapi: 3.0.0
info:
  title: Bundle test fixture
  version: version.from.tag
channels:
  testChannel:
    address: test/channel
    description:
      $ref: ./description.md
    messages:
      testMessage:
        $ref: '#/components/messages/TestMessage'
components:
  messages:
    TestMessage:
      payload:
        schemaFormat: application/schema+json;version=draft-07
        schema:
          $ref: ./schema.json
      examples:
        - $ref: ./example.json
